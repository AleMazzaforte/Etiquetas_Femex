<%- include('partials/head'); -%>
<body>
  <%- include('partials/header'); -%>

  <form id="form" action="/gestion/mostrarRma" method="GET">
      <label for=""><h2>Cliente</h2></label>
      <input type="text" name="cliente" value="<%= cliente ? cliente.nombre : '' %>">
      <input type="submit" value="Buscar">
  </form><br>

  <div id="mostrarBusqueda"></div>

  <div id="tabla">
    <table>
      <thead>
        <tr>
          <th>Cliente</th>
          <th>Modelo</th>
          <th>Cantidad</th>
          <th>Solicita</th>
          <th>Op/Lote</th>
          <th>Vencimiento</th>
          <th>Se Entrega</th>
          <th>Se Recibe</th>
          <th>Observación</th>
          <th>Número de Ingreso</th>
          <th>Número de Egreso</th>
        </tr>
      </thead>
      <tbody id="productos-body">
        <!-- El contenido de los productos se cargará aquí -->
      </tbody>
    </table>
  </div>

  <!-- Pasar datos del servidor al script -->
  <script>
    const productosData = <%- JSON.stringify(productos || []) %>;
    const clienteNombre = "<%= cliente ? cliente.nombreCliente : '' %>";
  </script>
  <script src="/js/js.js"></script>
</body>
</html>


